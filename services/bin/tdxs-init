#!/bin/bash
set -e

own_file() {
    [ -e $1 ] && { chown root:tdx $1 && chmod 660 $1; } || true
}

own_file /dev/tpm0
own_file /dev/tpmrm0
own_file /dev/tdx_guest
own_file /sys/kernel/security/tpm0/binary_bios_measurements
